@model IEnumerable<ExcellentTaste.Models.Bestelling>

@{
    ViewBag.Title = "Index";
}

<h2>Overzicht bestellingen</h2>

<p>
    @Html.ActionLink("Toevoegen", "Create", "Bestelling", null, new { @class = "btn btn-primary btn-large" })
</p>
<table class="table">
    <tr>
        <th>
            Tafel
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ConsumptieItem.consumptieGroepCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ConsumptieItem.consumptieItemNaam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.aantal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Reservering.tafel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ConsumptieItem.consumptieGroepCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ConsumptieItem.consumptieItemNaam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.aantal)
        </td>
        <td>
            @Html.ActionLink("Wijzig", "Edit", new { id = item.bestellingId }) |
            @Html.ActionLink("Details", "Details", new { id = item.bestellingId }) |
            @Html.ActionLink("Verwijder", "Delete", new { id = item.bestellingId })
        </td>
    </tr>
}

</table>
