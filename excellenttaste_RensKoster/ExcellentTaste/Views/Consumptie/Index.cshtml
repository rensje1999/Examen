@model IEnumerable<ExcellentTaste.Models.Consumptie>

@{
    ViewBag.Title = "Index";
}

<h2>Overzicht consumptie hoofdgroepen</h2>

<p>
    @Html.ActionLink("Toevoegen", "Create", "Consumptie", null, new { @class = "btn btn-primary btn-large" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.consumptieNaam)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.consumptieNaam)
        </td>
        <td>
            @Html.ActionLink("Wijzig", "Edit", new { id=item.consumptieCode }) |
            @Html.ActionLink("Details", "Details", new { id=item.consumptieCode }) |
            @Html.ActionLink("Verwijder", "Delete", new { id=item.consumptieCode })
        </td>
    </tr>
}

</table>
